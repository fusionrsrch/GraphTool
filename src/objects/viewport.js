export default class ViewPort {
  top;
  bottom;
  left;
  right;
  constructor(top, bottom, left, right) {
    this.top = top;
    this.bottom = bottom;
    this.left = left;
    this.right = right;
  }
}
